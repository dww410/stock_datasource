{
  "enabled": true,
  "rate_limit": 30,
  "timeout": 30,
  "retry_attempts": 3,
  "description": "TuShare 指数技术因子数据（100+ 技术指标）",
  "schedule": {
    "frequency": "daily",
    "time": "18:00"
  },
  "parameters": {
    "validate_prices": true,
    "require_ohlc": true
  },
  "parameters_schema": {
    "trade_date": {
      "type": "string",
      "format": "date",
      "required": false,
      "description": "Trade date in YYYYMMDD format"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "required": false,
      "description": "Start date in YYYYMMDD format"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "required": false,
      "description": "End date in YYYYMMDD format"
    },
    "ts_code": {
      "type": "string",
      "required": false,
      "description": "Index code (e.g., 000300.SH)"
    }
  },
  "cooling": {
    "rate_limit_seconds": 360,
    "ip_limit_seconds": 600
  }
}
